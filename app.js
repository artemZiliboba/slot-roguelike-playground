const SYMBOLS={cherry:{name:'Cherry',icon:'🍒',base:5,weight:10,tags:['fruit'],description:'Стоимость 5. Пара: 10. Тройка: 30.'},lemon:{name:'Lemon',icon:'🍋',base:8,weight:10,tags:['fruit'],description:'Стоимость 8. Пара: 16. Тройка: 48.'},bell:{name:'Bell',icon:'🔔',base:12,weight:8,tags:[],description:'Стоимость 12. Если совпали минимум 2 Bell, линия получает +16 base.'},diamond:{name:'Diamond',icon:'💎',base:10,weight:3,tags:['gem'],description:'Стоимость 10. Пара: 20. Тройка: 60. Gem Polish усиливает линию ×1.5.'},bomb:{name:'Bomb',icon:'💣',base:0,weight:5,tags:['destruction'],description:'Base 0, но совпавшая Bomb даёт +15 bonus за каждую Bomb.'},wild:{name:'Wild',icon:'★',base:0,weight:2,tags:['wild'],description:'Заменяет символ для пары/тройки, но сам добавляет 0 base points.'}};
const MODIFIERS={fruitBasket:{name:'Fruit Basket',description:'Cherry и Lemon получают +4 стоимости.'},doubleGlass:{name:'Double Glass',description:'Если центральная клетка выигрышная, её стоимость добавляется ещё раз.'},gemPolish:{name:'Gem Polish',description:'Выигрышная линия Diamond получает ×1.5.'},scrapCollector:{name:'Scrap Collector',description:'Каждая активированная Bomb дополнительно даёт +2.'},echoChamber:{name:'Echo Chamber',description:'Первый bonus trigger линии повторяется ещё раз.'}};
const initialPool=()=>({cherry:3,lemon:2,bell:2,diamond:1,wild:1,bomb:0});let state,forcedGrid=null;
function makeState(seed){return{seed:normalizeSeed(seed),rngState:normalizeSeed(seed),pool:initialPool(),modifiers:new Set(),score:0,target:120,spins:8,coins:20,lastScore:0,lastHits:new Set(),events:[],breakdown:[],reward:null};}
function normalizeSeed(v){const n=Number(v);return Number.isFinite(n)?(Math.abs(Math.floor(n))>>>0)||1:1;}function random(){state.rngState=(state.rngState+0x6D2B79F5)>>>0;let t=state.rngState;t=Math.imul(t^(t>>>15),t|1);t^=t+Math.imul(t^(t>>>7),t|61);return((t^(t>>>14))>>>0)/4294967296;}
function weightedPick(){const es=Object.entries(state.pool).filter(([,c])=>c>0).map(([id,c])=>({id,weight:SYMBOLS[id].weight*c})),total=es.reduce((s,e)=>s+e.weight,0);let r=random()*total;for(const e of es){r-=e.weight;if(r<=0)return e.id;}return es.at(-1).id;}function makeGrid(){if(forcedGrid){const g=forcedGrid;forcedGrid=null;return g;}return Array.from({length:9},weightedPick);}function symbolBase(id){let v=SYMBOLS[id].base;if(state.modifiers.has('fruitBasket')&&SYMBOLS[id].tags.includes('fruit'))v+=4;return v;}
function resolveLine(grid,indexes,row){const ids=indexes.map(i=>grid[i]),nonWild=ids.filter(id=>id!=='wild'),counts={};nonWild.forEach(id=>counts[id]=(counts[id]||0)+1);let candidate=null,best=0;for(const[id,c]of Object.entries(counts))if(c>best){candidate=id;best=c;}const wilds=ids.filter(id=>id==='wild').length;if(!candidate||best+wilds<2)return{score:0,matchedPositions:[],events:[],display:{row,symbols:ids.map(id=>SYMBOLS[id].icon).join(' '),title:'Нет комбинации',formula:'Нет 2 одинаковых символов',score:0}};const matched=indexes.filter((idx,l)=>ids[l]===candidate||ids[l]==='wild'),matchCount=Math.min(3,best+wilds),parts=ids.map(id=>id===candidate?symbolBase(id):0),rawBase=parts.reduce((a,b)=>a+b,0);let base=rawBase,mult=matchCount===3?2:1;const events=[`LINE_MATCHED row=${row} ${SYMBOLS[candidate].name} x${matchCount}: base ${base} × ${mult}`],notes=[];if(candidate==='bell'&&best>=2){base+=16;notes.push('Bell bonus +16 base');events.push('Bell pair bonus: +16 base');}if(candidate==='diamond'&&state.modifiers.has('gemPolish')){mult*=1.5;notes.push('Gem Polish ×1.5');events.push('Gem Polish: Diamond line ×1.5');}let score=Math.floor(base*mult),bonuses=[];if(candidate==='bomb'){let amount=best*15;if(state.modifiers.has('scrapCollector'))amount+=best*2;bonuses.push({name:`Bomb activation ×${best}`,amount});}if(state.modifiers.has('doubleGlass')&&matched.includes(4)&&grid[4]!=='wild')bonuses.push({name:'Double Glass',amount:symbolBase(grid[4])});if(state.modifiers.has('echoChamber')&&bonuses.length)bonuses.push({name:`Echo Chamber repeats ${bonuses[0].name}`,amount:bonuses[0].amount});for(const b of bonuses){score+=b.amount;notes.push(`${b.name} +${b.amount}`);events.push(`${b.name}: +${b.amount}`);}events.push(`SCORE_ADDED row=${row}: +${score}`);let formula=`(${parts.join(' + ')}`+(candidate==='bell'&&best>=2?' + 16':'')+`) × ${mult}`;if(bonuses.length)formula+=` + ${bonuses.map(b=>b.amount).join(' + ')}`;return{score,matchedPositions:matched,events,display:{row,symbols:ids.map(id=>SYMBOLS[id].icon).join(' '),title:`${SYMBOLS[candidate].name} ×${matchCount}`,formula,notes,score}};}
function spin(){if(state.spins<=0||state.score>=state.target)return;state.events=['SPIN_STARTED'];state.lastHits=new Set();state.reward=null;const grid=makeGrid();state.events.push(`GRID_CREATED rngState=${state.rngState}`);let total=0;state.breakdown=[];[[0,1,2],[3,4,5],[6,7,8]].forEach((line,row)=>{const r=resolveLine(grid,line,row+1);state.breakdown.push(r.display);total+=r.score;r.matchedPositions.forEach(i=>state.lastHits.add(i));state.events.push(...r.events);});state.spins--;state.lastScore=total;state.score+=total;state.events.push(`SPIN_FINISHED total=${total}`);state.grid=grid;if(state.score>=state.target){const baseReward=6,spinBonus=state.spins*2,reward=baseReward+spinBonus,before=state.coins;state.coins+=reward;state.reward={baseReward,remaining:state.spins,spinBonus,reward,before,after:state.coins};state.events.push(`ENCOUNTER_WON reward=${reward} coins`);}else if(state.spins===0)state.events.push('ENCOUNTER_LOST');render();}
function renderGrid(){const el=document.querySelector('#grid'),g=state.grid||Array(9).fill(null);el.innerHTML=g.map((id,i)=>id?`<div class="cell ${state.lastHits.has(i)?'hit':''}"><div class="icon">${SYMBOLS[id].icon}</div><div class="name">${SYMBOLS[id].name}</div></div>`:`<div class="cell"><div class="icon">?</div><div class="name">empty</div></div>`).join('');}
function renderBreakdown(){const el=document.querySelector('#scoreBreakdown');if(!state.breakdown.length){el.innerHTML='<div class="empty-breakdown">После Spin здесь будет расчёт очков по каждой строке.</div>';return;}el.innerHTML=state.breakdown.map(x=>`<div class="break-row ${x.score?'win':'lose'}"><div><span class="row-name">Ряд ${x.row}</span><b>${x.symbols}</b></div><div class="calc"><strong>${x.title}</strong><span>${x.formula}</span>${x.notes?.length?`<small>${x.notes.join(' · ')}</small>`:''}</div><div class="line-score">+${x.score}</div></div>`).join('')+`<div class="spin-total"><span>Итого за Spin</span><strong>+${state.lastScore}</strong></div>`;}
function renderReward(){const el=document.querySelector('#rewardPanel');if(!state.reward){el.classList.add('hidden');el.innerHTML='';return;}const r=state.reward;el.classList.remove('hidden');el.innerHTML=`<h2>Encounter Complete</h2><div class="reward-grid"><span>Цель достигнута</span><strong>${state.score} / ${state.target}</strong><span>Базовая награда</span><strong>+${r.baseReward} 🪙</strong><span>Осталось Spin: ${r.remaining} × 2</span><strong>+${r.spinBonus} 🪙</strong><span>Итого Coins</span><strong>+${r.reward} 🪙</strong><span>Баланс</span><strong>${r.before} → ${r.after} 🪙</strong></div>`;}
function render(){renderGrid();renderBreakdown();renderReward();document.querySelector('#targetText').textContent=`${state.score} / ${state.target}`;document.querySelector('#scoreProgress').style.width=`${Math.min(100,state.score/state.target*100)}%`;document.querySelector('#spinsText').textContent=state.spins;document.querySelector('#coinsText').textContent=state.coins;document.querySelector('#lastScoreText').textContent=`+${state.lastScore}`;document.querySelector('#seedInput').value=state.seed;document.querySelector('#spinBtn').disabled=state.spins<=0||state.score>=state.target;document.querySelector('#resultText').textContent=state.score>=state.target?'Цель достигнута! Награда Coins показана ниже.':state.spins===0?'Spin закончились раньше Target — encounter проигран.':`Encounter Score: ${state.score} / ${state.target}`;document.querySelector('#poolList').innerHTML=Object.entries(state.pool).filter(([,c])=>c>0).map(([id,c])=>`<div class="pool-row"><div><span>${SYMBOLS[id].icon} ${SYMBOLS[id].name}</span><small>${SYMBOLS[id].description}</small></div><strong>×${c}</strong></div>`).join('');document.querySelector('#modifierList').innerHTML=state.modifiers.size?[...state.modifiers].map(id=>`<div class="modifier-item"><strong>${MODIFIERS[id].name}</strong><small>${MODIFIERS[id].description}</small></div>`).join(''):'<span class="muted">Нет modifiers.</span>';document.querySelector('#eventLog').innerHTML=state.events.slice().reverse().map(e=>`<li>${e}</li>`).join('');}
function log(m){state.events.push(m);if(state.events.length>50)state.events.shift();}function addSymbol(id){state.pool[id]=(state.pool[id]||0)+1;log(`DEV: added ${SYMBOLS[id].name}`);render();}function toggleModifier(id){state.modifiers.has(id)?state.modifiers.delete(id):state.modifiers.add(id);log(`DEV: toggled ${MODIFIERS[id].name}`);render();}
function boot(seed=12345){state=makeState(seed);document.querySelector('#symbolButtons').innerHTML=Object.keys(SYMBOLS).map(id=>`<button data-symbol="${id}">+ ${SYMBOLS[id].icon} ${SYMBOLS[id].name}</button>`).join('');document.querySelector('#modifierButtons').innerHTML=Object.keys(MODIFIERS).map(id=>`<button data-modifier="${id}">${MODIFIERS[id].name}</button>`).join('');render();}
document.querySelector('#spinBtn').onclick=spin;document.querySelector('#restartSeedBtn').onclick=()=>boot(document.querySelector('#seedInput').value);document.querySelector('#newSeedBtn').onclick=()=>boot(Date.now()>>>0);document.querySelector('#resetRunBtn').onclick=()=>boot(state.seed);document.querySelector('#addCoinsBtn').onclick=()=>{state.coins+=20;log('DEV: +20 Coins');render();};document.querySelector('#clearLogBtn').onclick=()=>{state.events=[];render();};document.querySelector('#forceJackpotBtn').onclick=()=>{forcedGrid=['cherry','cherry','wild','diamond','diamond','wild','bomb','bomb','wild'];log('DEV: forced test grid armed');render();};document.querySelector('#symbolButtons').onclick=e=>{if(e.target.dataset.symbol)addSymbol(e.target.dataset.symbol);};document.querySelector('#modifierButtons').onclick=e=>{if(e.target.dataset.modifier)toggleModifier(e.target.dataset.modifier);};boot();